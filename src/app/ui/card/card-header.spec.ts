import { CardHeader } from './card-header';

describe('CardHeader', () => {
  it('should create an instance', () => {
    const directive = new CardHeader();
    expect(directive).toBeTruthy();
  });
});
