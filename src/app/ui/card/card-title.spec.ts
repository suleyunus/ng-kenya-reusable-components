import { CardTitle } from './card-title';

describe('CardTitle', () => {
  it('should create an instance', () => {
    const directive = new CardTitle();
    expect(directive).toBeTruthy();
  });
});
