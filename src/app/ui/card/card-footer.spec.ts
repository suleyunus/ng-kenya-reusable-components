import { CardFooter } from './card-footer';

describe('CardFooter', () => {
  it('should create an instance', () => {
    const directive = new CardFooter();
    expect(directive).toBeTruthy();
  });
});
