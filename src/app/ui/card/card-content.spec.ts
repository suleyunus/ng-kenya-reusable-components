import { CardContent } from './card-content';

describe('CardContent', () => {
  it('should create an instance', () => {
    const directive = new CardContent();
    expect(directive).toBeTruthy();
  });
});
