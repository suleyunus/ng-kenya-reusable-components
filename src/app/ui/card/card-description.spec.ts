import { CardDescription } from './card-description';

describe('CardDescription', () => {
  it('should create an instance', () => {
    const directive = new CardDescription();
    expect(directive).toBeTruthy();
  });
});
